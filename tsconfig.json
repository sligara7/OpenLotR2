{
  "compileOnSave": false,
  "compilerOptions": {
    "outDir": "./dist/out-tsc",
    "sourceMap": true,
    "declaration": false,
    "moduleResolution": "node",
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "skipLibCheck": true,
    "target": "es5",
    "typeRoots": ["node_modules/@types"],
    "lib": ["es2017", "es2016", "es2015", "dom"]
  },
  "include": ["src/**/*"],
  "comment:exclude": "src/game is the headless sim core: it uses .ts import extensions (bundler resolution) and is type-checked via tsconfig.typecheck.json and run via tsx, so it is excluded from the legacy electron-webpack build until the bundler is modernised.",
  "exclude": ["node_modules", "**/*.tsx", "src/game"]
}
